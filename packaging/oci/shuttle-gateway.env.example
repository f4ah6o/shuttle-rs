# Shuttle Gateway container runtime environment.
# Keep real values in a secret manager or pass them with runtime env flags.

# Owner approval token for OAuth listeners.
SHUTTLE_OAUTH_ADMIN_TOKEN=

# Bearer token for private listeners that use auth = "bearer".
SHUTTLE_GATEWAY_TOKEN=

# Backend bearer token for [projects.main].token_env.
SHUTTLE_MAIN_BACKEND_TOKEN=
