[package]
name = "stl"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

# The published `stl` binary is built from the root `shuttle-rs` package; this
# development mirror emits `stl-dev` so the two targets don't collide in the
# shared workspace target directory.
[[bin]]
name = "stl-dev"
path = "src/main.rs"

[dependencies]
anyhow.workspace = true
chrono.workspace = true
clap.workspace = true
futures-executor.workspace = true
serde.workspace = true
serde_json.workspace = true
shuttle-app = { path = "../shuttle-app" }
shuttle-context = { path = "../shuttle-context" }
shuttle-core = { path = "../shuttle-core" }
shuttle-memory = { path = "../shuttle-memory" }
shuttle-message = { path = "../shuttle-message" }
shuttle-mesh = { path = "../shuttle-mesh" }
shuttle-remote = { path = "../shuttle-remote" }
shuttle-mcp = { path = "../shuttle-mcp" }
shuttle-store = { path = "../shuttle-store" }
shuttle-task = { path = "../shuttle-task" }
tokio.workspace = true
uuid.workspace = true

[dev-dependencies]
tempfile.workspace = true
